import { PlaceholderSection } from "@/components/dashboard/PlaceholderSection";

export default function PaymentsPage() {
  return (
    <PlaceholderSection
      title="Payments"
      description="Review shipping and service charges. Stripe checkout will be connected in a later step."
    />
  );
}
